#ifndef VARIABLE_H
#define VARIABLE_H

extern bool start;
extern bool ready;
extern bool okmove;
extern bool mode_hal;
extern bool mode_feedback;
extern bool mode_ikfrominit;

class variable
{
public:
    variable() = default;
};

#endif // VARIABLE_H
